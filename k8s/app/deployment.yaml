apiVersion: apps/v1
kind: Deployment
metadata:
  name: smallbiz-app
  namespace: smallbiz
  labels:
    app: smallbiz-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smallbiz-app
  template:
    metadata:
      labels:
        app: smallbiz-app
    spec:
      containers:
        - name: app
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
              protocol: TCP
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secret
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 5
          volumeMounts:
            - name: app-html
              mountPath: /usr/share/nginx/html
      volumes:
        - name: app-html
          configMap:
            name: app-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-html
  namespace: smallbiz
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="ko">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>SmallBiz Platform</title>
      <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
          font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
          background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
          color: #e2e8f0;
          min-height: 100vh;
          display: flex;
          align-items: center;
          justify-content: center;
        }
        .container {
          text-align: center;
          padding: 3rem;
          background: rgba(255,255,255,0.05);
          border-radius: 1rem;
          border: 1px solid rgba(255,255,255,0.1);
          max-width: 600px;
        }
        h1 { font-size: 2.5rem; margin-bottom: 1rem; }
        .emoji { font-size: 4rem; margin-bottom: 1.5rem; }
        .stack {
          display: flex;
          flex-wrap: wrap;
          gap: 0.5rem;
          justify-content: center;
          margin-top: 1.5rem;
        }
        .badge {
          background: rgba(99,102,241,0.2);
          border: 1px solid rgba(99,102,241,0.4);
          padding: 0.25rem 0.75rem;
          border-radius: 9999px;
          font-size: 0.85rem;
        }
        .status { color: #4ade80; margin-top: 1.5rem; }
        .footer { margin-top: 2rem; font-size: 0.8rem; color: #64748b; }
      </style>
    </head>
    <body>
      <div class="container">
        <div class="emoji">🏢</div>
        <h1>SmallBiz Platform</h1>
        <p>소규모 기업을 위한 클라우드 인프라 플랫폼</p>
        <div class="stack">
          <span class="badge">EKS 1.31</span>
          <span class="badge">Terraform</span>
          <span class="badge">ArgoCD</span>
          <span class="badge">ALB Ingress</span>
          <span class="badge">RDS MySQL</span>
          <span class="badge">GitOps</span>
        </div>
        <p class="status">✅ All systems operational</p>
        <p class="footer">Built by Byeongki Cho | AWS Portfolio Project</p>
      </div>
    </body>
    </html>
